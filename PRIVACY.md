# JackVoice 隐私说明

更新日期：2026-08-09

这份说明描述官方源代码默认实现的数据流。第三方分发者可能修改程序，使用前请同时核对其源码、签名和发布说明。

## 一、JackVoice 处理什么数据

### 麦克风音频

用户主动开始听写后，JackVoice 才会读取所选麦克风。音频会在本机进行格式转换和 WebRTC 自动增益处理，随后通过 TLS/WSS 连接发送到用户自行配置的火山引擎豆包语音识别服务。

JackVoice 项目不运营中转服务器，也不接收这部分音频。云端服务如何留存、使用或删除数据，由用户的火山引擎账户配置、服务协议及隐私政策决定。

### 转写文本与本地录音

转写文本会保存在本机，用于首页历史。WAV 录音的策略由用户选择：

- 不保存
- 保存 7 天
- 保存 30 天（全新安装默认）
- 永久保存

旧版本没有录音保留设置，因此升级时会先沿用永久保留，避免在用户未选择前静默删除已有文件。修改策略后，程序会立即清理已过期的关联录音。逐条删除历史会删除该条文本、录音和关联旧备份；“清空全部”会删除所有历史文本、录音和历史备份。普通文件系统删除不等同于对磁盘执行法证级安全擦除，系统备份、文件系统快照或云盘同步也可能保留副本。

### 词库

热词和替换词默认保存在本机。听写请求可能把有限数量的热词发送给识别服务；配置云端热词表 ID 后，保存或手动同步热词会把热词表内容上传到火山引擎。替换词只在本机进行确定性文本替换，不上传到云端热词表。

### 凭据与设置

火山引擎 APP Key 存入操作系统凭据库：macOS 使用 Keychain，Windows 使用系统凭据存储。资源 ID、热词表 ID、快捷键、麦克风选择、录音保留策略等非秘密设置保存在本机设置文件。

从早期版本升级时，程序会尝试把设置文件中的旧 APP Key 迁移到系统凭据库，并重写设置文件、删除旧设置备份。操作系统备份或版本控制历史中的旧副本不在应用的控制范围内，发布代码前仍应运行凭据扫描。

## 二、本地数据位置

macOS 默认业务数据目录：

```text
~/Library/Application Support/com.jackvoice.shared
```

操作系统还可能在 Keychain、应用容器、日志、备份或系统诊断数据中保存相关信息。JackVoice 不主动采集遥测、广告标识符或崩溃报告；如果第三方发行版增加此类功能，应另行披露。

## 三、权限用途

- 麦克风：只用于用户主动发起的听写和麦克风测试
- 辅助功能：判断是否存在可编辑焦点，并在安全时模拟粘贴
- 全局快捷键相关权限：在其他应用前台时开始或结束听写
- 开机启动：仅在用户启用该选项后使用

## 四、用户控制

用户可以在“设置 → 数据与隐私”中改变录音保留策略或清空全部历史，也可以在首页逐条删除。删除 APP Key 时，将识别设置中的 APP Key 留空并保存即可。

卸载应用不会保证自动清除所有数据；如需彻底移除，应同时删除本地数据目录和操作系统凭据库中的 `com.jackvoice.shared` / `volc-api-key` 条目，并检查系统备份。

## 五、网络服务

默认源码只在用户触发对应功能时连接火山引擎豆包语音接口。JackVoice 与文中提及的第三方服务商不存在隶属、授权或背书关系。

## 六、问题与变更

隐私或安全问题请按 [SECURITY.md](SECURITY.md) 提交。数据流发生实质变化时，应同时更新本文件、首次启动披露和应用内说明。
