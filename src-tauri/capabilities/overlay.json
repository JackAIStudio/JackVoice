{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay",
  "description": "Minimal capability for the dictation overlay",
  "windows": ["overlay"],
  "permissions": ["core:default"]
}
