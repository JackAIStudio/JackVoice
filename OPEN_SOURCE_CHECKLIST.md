# 开源发布检查清单

这份清单区分了已经由代码解决的问题，以及必须由仓库所有者在正式公告或发布二进制前确认的事项。

## 代码和产品

- [x] 首页不再使用“个性化问候 + 四张统计卡 + 同构历史列表”的竞品式结构
- [x] API Key 改存操作系统凭据库，旧明文设置会自动迁移
- [x] 正式构建拒绝三处版本号漂移，并在构建前弹出当前产物目录中的旧 DMG 挂载
- [x] 正式交付 DMG 使用构建标识与内容哈希唯一命名，同时生成 SHA-256 和 JSON 清单
- [x] 用户可以逐条删除或清空应用内历史索引，录音文件由用户在系统文件夹中自行管理
- [x] 每次听写优先保存本地 WAV，应用不自动到期或删除录音
- [x] 首次启动明确披露云端识别与本地永久保存方式
- [x] Tauri 已启用 CSP、关闭全局 API 注入并缩小前端 capability
- [x] 移除仓库中的个人 Developer ID 和过宽 macOS entitlement
- [x] 正式版构建强制使用 Developer ID，并拒绝 ad-hoc、缺失 Team ID 或不完整的应用包签名
- [x] 添加许可证、隐私、安全、贡献、行为准则和第三方说明

## 所有者必须确认

- [x] 2026-08-10 使用 Gitleaks 8.30.1 扫描当前跟踪文件和全部 Git 历史，均无命中
- [ ] 轮换任何曾进入提交、日志、截图、Release 或 CI 的真实 API Key、Token、证书和密码
- [x] 当前公开仓库为单一整理提交，作者使用 GitHub noreply 邮箱，不需要为个人提交信息重写历史
- [ ] 确认所有 Logo、图标、截图、字体和演示音频均为原创或拥有可再分发授权
- [ ] 在计划发布的国家/地区检索 `JackVoice` 名称的商标、公司名、域名和应用商店冲突
- [ ] 确认火山引擎账号和模型的使用方式、配额、数据处理及向最终用户披露符合服务条款
- [x] GitHub 已启用 Private vulnerability reporting、Dependabot、Secret scanning 和 Push protection
- [x] 默认分支已要求通过 PR 合入、解决评审对话并通过前端、Rust 和安全审计检查；管理员同样受规则约束
- [x] 仓库跟踪集不含安装包、录音、崩溃日志、开发数据库或其他个人数据；构建目录与本机产物已明确排除
- [ ] 在干净机器或 CI 上完成 macOS 构建、签名、公证和安装验证；Windows 标记为实验支持，直到完成真实端到端验证
- [ ] 核对“关于”页构建标识、交付清单 `buildId` 与待发布 DMG 文件名三者一致
- [ ] 发布二进制时，从锁文件生成并随包附带完整第三方许可清单

> 扫描无命中不能证明从未在仓库之外泄漏过真实凭据。只要真实凭据曾出现在日志、截图、Release、CI 或共享目录中，仍应在服务端轮换。

## 历史重写提示

重写公开前的私有历史会改变所有提交 ID，也要求其他克隆重新同步。不要在未备份、未轮换泄漏凭据或未通知协作者时直接强推。即使重写历史，已经被第三方抓取的秘密也必须视为泄漏并在服务端撤销。
