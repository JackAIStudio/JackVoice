# 安全政策

## 支持范围

项目仍处于早期阶段。安全修复优先进入默认分支；旧版本不承诺长期维护。请先用最新代码复现问题。

## 报告漏洞

请不要在公开 Issue 中披露未修复的漏洞、真实 App Key、录音或私人转写文本。

仓库公开后，优先使用 GitHub 的 **Security → Report a vulnerability** 私密报告入口。若该入口尚未启用，请通过仓库维护者 GitHub 主页中公开的私密联系方式联系，并包含：

- 受影响版本、操作系统和架构
- 最小复现步骤与影响范围
- 必要的日志或代码位置（先移除凭据和个人数据）
- 已知缓解方案（如有）

维护者确认问题并准备修复前，请给予合理的协调披露时间。

## 凭据泄露

如果 App Key、签名证书、私钥或发布 Token 曾进入 Git 历史：

1. 先在服务端撤销或轮换凭据；只从当前文件删除是不够的。
2. 检查提交、Tag、Release 附件、CI 日志、Issue 和 Fork。
3. 再评估是否需要使用 `git-filter-repo` 重写历史并通知协作者重新克隆。

程序代码不会记录完整 App Key；错误报告也不应包含真实凭据。
